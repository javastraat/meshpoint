{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Meshpoint",
        "description": "Enter your Meshpoint gateway's address. If \"Require authentication\" is on under Meshpoint's Configuration → Metrics, also generate an API key there and paste it below. That key grants read-only access to three status endpoints (packet/node stats, system health, and the richer stats-page data) -- never write access, never mesh/node data.",
        "data": {
          "host": "Host or IP address",
          "port": "Port",
          "api_key": "API key (leave blank if authentication is off)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach Meshpoint at that address.",
      "invalid_auth": "Meshpoint rejected the API key.",
      "metrics_disabled": "The /metrics endpoint is disabled — enable it in Meshpoint's Configuration → Metrics page first.",
      "unknown": "Unexpected error."
    },
    "abort": {
      "already_configured": "This Meshpoint gateway is already configured."
    }
  }
}
